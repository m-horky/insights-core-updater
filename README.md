# insights-core-updater

A tool to autoupdate Insights Core, unrelated to insights-client. An experiment.

## Using

```shell
# Development
$ cargo run
# Production
$ cargo build --release
$ ./target/release/insights_core_updater
$ DEBUG=true ./target/release/insights_core_updater
```
